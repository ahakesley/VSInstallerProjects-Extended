<Window x:Class="SetupProjectHelper.Views.VdprojFilePropertyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:SetupProjectHelper.ViewModels"
        xmlns:views="clr-namespace:SetupProjectHelper.Views"
        mc:Ignorable="d"
        Title="{Binding FileEntryViewModel.Name}"
        SizeToContent="WidthAndHeight">
    <Window.DataContext>
        <viewModels:VdprojFilePropertyViewModel/>
    </Window.DataContext>

    <Grid IsSharedSizeScope="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition SharedSizeGroup="KeyColumn"/>
            <ColumnDefinition SharedSizeGroup="ValueColumn"/>
        </Grid.ColumnDefinitions>
        <views:AutoPropertyGrid TargetObject="{Binding FileEntryViewModel}" Grid.ColumnSpan="2" Grid.IsSharedSizeScope="True"/>


    </Grid>
</Window>
